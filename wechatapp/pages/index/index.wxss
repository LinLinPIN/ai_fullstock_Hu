page{
    font-size:32rpx;
    line-height:1.5;
}
image{
    display:block;
    width:100%;
}
.hero image{
    height:38.2vh;/*图片的宽高比,等比例，不能模糊*/
}
.hero .white{
    background-color: #ffff;
}
.hero .content{
    padding-top:10%;
}
.hero{height:80vh;}
.hero .header{
    font-size: 72rpx;
    font-weight:bold;
    margin:0 12% 16rpx;/*顺时针 不写的话默认和对应的边距相等（右不写默认和左外边距相等）*/
}
.hero .sub-header{

    font-size:32rpx;
    letter-spacing: 6rpx;
    margin-bottom: 16rpx;
    color:rgba(0,0,0,0.85);
}
.hero .description{
    padding:0 12%;
    margin-bottom: 64rpx;
}
.hero .content.centered{
    text-align: center;
}