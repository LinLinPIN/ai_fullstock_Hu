import React from 'react';
import PropTypes from 'prop-types';

const Publish = props => {
    return (
        <div>
            Publish
        </div>
    );
};

Publish.propTypes = {

};

export default Publish;